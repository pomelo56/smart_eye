# 版本管理规范

> 慧眼 SmartEye 采用语义化版本（Semantic Versioning）+ git tag 管理版本。

## 版本号格式

`MAJOR.MINOR.PATCH+BUILD`

| 字段 | 含义 | 何时递增 |
|------|------|---------|
| MAJOR | 不兼容的架构变更 | 大重构、技术栈更换 |
| MINOR | 新功能，向下兼容 | 新增平台、新交互、新语音 |
| PATCH | Bug 修复，向下兼容 | 修 OCR 误识别、修音频时序 |
| BUILD | 构建号（整数） | 每次发布递增 |

## 当前版本

**v0.8.3** (build 17) — 2026-07-10

## v1.0 发布冻结声明

在以下 checklist **全部完成**之前，禁止将 `pubspec.yaml` 的 major 版本提升到 `1`，也禁止发布 `v1.0.0` 标签：

- [ ] NSF（No Silent Failure）红线全量审计通过：所有 `catch` / 失败分支均有语音反馈
- [ ] 完成 5 款以上主流机型真机兼容性测试（含 OPPO / vivo / 小米 / 华为 / 荣耀）
- [ ] README.md / PRD.md / HANDOVER.md / TASKBOARD.md 与当前代码一致
- [ ] 生产签名配置验证通过，release APK 使用正式 keystore
- [ ] 用户无障碍走查通过（至少 2 位视障用户试用并反馈）
- [ ] 应用内更新、日志导出、权限流程在 Android 10+ 真机验证通过
- [ ] 核心维护者人工批准（不可由 AI 单独决策）

`scripts/release.sh` 已内置硬拦截：当 tag 或 `pubspec.yaml` 的 major >= 1 时，必须显式设置 `ALLOW_V1_RELEASE=1` 才能继续。

## 版本历史

| 版本 | 日期 | 标签 | 说明 |
|------|------|------|------|
| v0.8.3 | 2026-07-10 | `v0.8.3` | 应用内更新、版本治理门禁 |
| v0.8.2 | 2026-07-10 | `v0.8.2` | （预留：如需补充可在此填写） |
| v0.7.2 | 2026-07-07 | `v0.7.2` | **无障碍增强**：弱光自动检测 + 手电自动开启 |
| v0.2.0 | 2026-06 | `v0.2.0` | Flutter MVP 迁移 |
| v0.3.0 | 2026-06 | `v0.3.0` | 版本管理体系与 Bug 修复 |
| v0.4.0 | 2026-07-04 | `v0.4.0` | 多码检测+平台识别+语音优化+App图标 |
| v0.5.0 | 2026-07-04 | `v0.5.0` | 原生音频回调+历史分条播放+代码质量改进 |
| v0.5.1 | 2026-07-05 | `v0.5.1` | 高优先级 Bug 修复：音频初始化、stop 反馈、日志重建、OCR 冷却泄漏 |
| v0.6.0 | 2026-07-05 | `v0.6.0` | 「发现外卖」平台关键词提示：5 秒冷却，3 段音频拼接 |
| v0.6.1 | 2026-07-06 | `v0.6.1` | APK 体积优化：ABI 过滤 + 孤儿音频清理，65 MB → 31.6 MB |
| v0.6.2 | 2026-07-06 | `v0.6.2` | Debug APK 体积优化：237 MB → 89 MB（ABI 限制在 debug 同样生效）|
| v0.7.0 | 2026-07-07 | `v0.7.0` | R8 代码压缩启用：30 MB 体积 + 多 Agent 任务看板 (`docs/TASKBOARD.md`) |
| v0.7.1 | 2026-07-07 | `v0.7.1` | **紧急修复**：摄像头权限缺失时不再静默卡死。PermissionService + 永久拒绝引导 + 3 段语音提示 |
| v0.7.2 | 2026-07-07 | `v0.7.2` | **无障碍增强**：弱光自动检测 + 手电自动开启。LuminanceDetector + TorchController + 3 段语音 |

## 发布流程

每次迭代完成后：

1. 更新 `pubspec.yaml` 的 `version` 字段
2. 更新 `CHANGELOG.md`，将 `[Unreleased]` 归档为版本号
3. 更新本文件的「当前版本」和「版本历史」
4. 提交：`git commit -m "release: vX.Y.Z"`
5. 打标签：`git tag vX.Y.Z`
6. 推送：`git push && git push --tags`

## 回滚方法

```bash
# 查看所有版本
git tag -l

# 回滚到某个版本（保留工作区）
git checkout vX.Y.Z -- .

# 回滚到某个版本（完整重置）
git reset --hard vX.Y.Z
```

## 下个版本计划 (v0.8.4 / v0.9.0)

- [ ] 同步 README.md / PRD.md / HANDOVER.md / TASKBOARD.md 到 v0.8.3 实际状态
- [ ] 完成 NSF 红线全量审计（P0 已修复，需审计剩余 `catch` 分支）
- [ ] Kotlin 版本回退到 Flutter 3.24 推荐版本（消除构建警告）
- [ ] 5 款以上主流机型兼容性测试
- [ ] TalkBack 焦点兼容性优化

## 长期规划

详见 [docs/ROADMAP.md](docs/ROADMAP.md)：

- v0.8.x：版本治理 + 构建稳定 + 文档同步
- v0.9.x：稳定性优化 + 真机兼容性验证 + 用户无障碍走查
- v1.0.0：正式版（**已冻结**，需完成上方 checklist 并由核心维护者人工批准）
- v1.1.0：系统 TTS 替代预录音频
- v2.0.0：多模态图片识别（识别非文字物品）
