- 诊断并修复 OPPO/ColorOS 设备上 TTS 无声的根因：系统 TTS 正常但 flutter_tts 默认引擎选择失败，导致 setLanguage 和 speak 均返回失败。
- 在 fix/tts-init-failure 分支上重构 TtsService：显式选择 TTS 引擎（优先 Google TTS）、setEngine 后等待 500ms、校验语言设置和测试 utterance、speak 时请求音频焦点。
- 更新 HomeScreen 启动流程：TTS 初始化失败时重试一次，并输出引擎/状态/可用引擎数等诊断日志。
- 清理 MainActivity.kt：移除未使用的自定义 TTS MethodChannel。
- 更新 TtsService 单元测试，覆盖引擎选择、语言失败、测试 utterance 失败等场景。
- 更新 CHANGELOG.md，记录 v0.1.0 和 Unreleased 修复内容。
- 根据实机日志进一步修复：OPPO/ColorOS 上 `flutter_tts.getEngines` 返回空列表，但系统默认引擎仍可工作。调整 TtsService 在 engines 为空时跳过 `setEngine()`，直接依赖系统默认引擎继续 `setLanguage` 和测试 `speak`。
- 更新对应单元测试。
- 16:08：用户指出多轮未解决，要求聚焦 P0 语音。决定放弃 `flutter_tts`，改用**预录音频 + Android MediaPlayer** 兜底方案。
  - 新建 `fix/audio-fallback` 分支，工作目录 `/Users/pomelo/Project/smart_eye_fix_audio`。
  - 使用 macOS `say` 生成中文音频：`num_0-9.mp3`、`jing.mp3`、`prefix.mp3`、`none.mp3`、`tutorial.mp3`。
  - 重写 `MainActivity.kt`：移除 TTS，实现 `com.smart_eye/audio` MethodChannel，用 `MediaPlayer` 顺序播放 assets 音频。
  - 新增 `lib/services/audio_service.dart`。
  - 重写 `lib/services/tts_service.dart`：将文本映射为音频序列，动态拼接取餐码数字音频。
  - 简化 `lib/screens/home_screen.dart`：保留相机/OCR，去掉历史记录和复杂 UI。
  - 更新 `pubspec.yaml`：移除 `flutter_tts`，声明 `assets/audio/`。
  - 更新 `test/unit/services/tts_service_test.dart`。
  - `flutter analyze` 零警告通过。沙箱环境 `flutter test` 因 WebSocket 异常无法执行，但 `flutter build apk --release` 成功（85.1MB）。
  - 输出 APK：`/Users/pomelo/Project/smart_eye/smarteye-audio-fallback.apk`。
- 22:46：实机安装 `smarteye-audio-fallback.apk` 后，日志显示 `Failed to play asset assets/audio/tutorial.mp3`，说明 MediaPlayer 找不到 asset 文件。
  - 根因：Flutter assets 打包到 APK 后位于 `assets/flutter_assets/assets/audio/...`，但原生代码直接用 `assets.openFd("assets/audio/...")` 访问，路径缺少 `flutter_assets/` 前缀。
  - 修复 `MainActivity.kt`：读取 asset 时自动 prepend `flutter_assets/`，Dart 层无需关心打包路径。
  - 重新构建 APK（85.1MB），覆盖原 `smarteye-audio-fallback.apk`。
  - 验证 APK 内文件列表，确认音频文件位于 `assets/flutter_assets/assets/audio/`。
  - amend 到 `fix/audio-fallback` 分支（`a30326b`），并更新 `CHANGELOG.md` 记录 asset 路径修复。
- 22:55：声音验证成功，用户要求继续补齐功能并生成未来 Agent 接手文档。
  - 创建 `docs/HANDOVER.md`，涵盖项目背景、技术方案、构建流程、已知陷阱、扩展语音指南、已实现/待实现清单。
  - 更新 `AGENTS.md`：替换 `flutter_tts` 为音频兜底方案、补充 asset 路径陷阱、更新目录结构和技术栈。
  - 实现 `HistoryService`：基于 `SharedPreferences`，最近 5 条，24 小时过期自动清理，含 6 个单元测试。
  - 实现手势：上滑播报历史记录，下滑播报操作帮助。
  - 实现距离反馈音：检测到文字时播放 `beep_slow.mp3`，识别到取餐码时播放 `beep_fast.mp3`。
  - 新增 `help.mp3` 操作帮助语音。
  - 为 `HomeScreen` 全屏添加 `Semantics` 标签，描述所有手势操作。
  - `flutter analyze` 零警告通过。沙箱环境 `flutter test` 仍因 WebSocket 异常无法执行。
  - 重新构建 APK（85.3MB），新提交 `fix/audio-fallback b2f7d86`。
  - 更新 `audio-fallback-diff.patch`。
